#include "Weapon.h"

Weapon::Weapon(int neededPoints, float damage, float rof, float distance, float spread, int ammo) :neededPoints(neededPoints), damage(damage), rof(rof), distance(distance), spread(spread), ammo(ammo) {}
